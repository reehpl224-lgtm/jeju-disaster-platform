{
  "name": "kma-weather-proxy-vercel",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "build": "echo no build step needed — /api is deployed as-is"
  },
  "devDependencies": {
    "@vercel/node": "^3.2.24",
    "typescript": "^5.7.2"
  }
}
