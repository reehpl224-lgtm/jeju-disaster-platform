# kma-weather-proxy(workers/kma-weather-proxy) 배포 후 나오는 Worker URL.
# 서비스키가 아니라 공개 프록시 주소라 커밋해도 안전합니다 — .env로 복사해 채워주세요.
VITE_WEATHER_PROXY_URL=https://jeju-kma-weather-proxy.<your-subdomain>.workers.dev
